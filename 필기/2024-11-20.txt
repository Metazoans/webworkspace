*proxy -> vue, express 프로젝트 개발에서만 사용
=> vue -> proxy -> express (포트 변경)

---------------------------------------------------------------------------------------------------------------------------------

Vue = Object
    => LifeCycle <-> Hook
    1) create => created : 초기화, server 통신, 이벤트 처리 등
    2) mounte => mounted : 초기화, 딜레이 ok
    3) update
    4) destroy => beforeDestroy : 마무리 작업, 가지고 있던 데이터 정리(임시 저장 or 서버로 보내기)

1) -> 객체 생성
2) -> DOM 연결
3) -> 화면 갱신
4) -> 화면 X, 객체 소멸

1,2,4 hook만 자주 사용, 3번은 무한 루프 위험 있음

---------------------------------------------------------------------------------------------------------------------------------








